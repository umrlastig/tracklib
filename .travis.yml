dist: xenial
language: python
branches:
  only:
    - main

python:
  - "3.7"

install:
  - pip install -r requirements.yml

script:
  - pytest test/
  - nosetests --with-coverage --cover-inclusive --with-doctest --cover-package=tracklib ./
  - cd doc
  - make html

after_success:
  - codecov

  
