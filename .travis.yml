dist: xenial
language: python
branches:
  only:
    - main

python:
  - "3.7"

install:
  - pip install -r requirements.yml

script:
  - pytest test/
  - coverage run --source=./tracklib -m pytest
  - cd doc
  - make html

after_success:
  - codecov

  
