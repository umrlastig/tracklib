[tool:pytest]
addopts =
    --cov-report term-missing --cov-branch --cov-report xml --cov-report term
    --cov=tracklib/ -vv --strict-markers -rfE

testpaths =
    test/

[coverage:run]
relative_files = true